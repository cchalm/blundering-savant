# GitHub Configuration
GITHUB_TOKEN=ghp_<your_github_token>

# Anthropic Configuration
ANTHROPIC_API_KEY=sk-ant-<your-anthropic-api-key>

# Bot Configuration
CHECK_INTERVAL=1m  # How often to check for new issues (e.g., 5m, 10m, 1h)
LOG_LEVEL=info     # Log level: debug, info, warn, error
RESUMABLE_CONVERSATIONS_DIR=./conversations

# The name of the GitHub workflow that the bot will use to validate code changes. Must have a `worflow_dispatch` event trigger
VALIDATION_WORKFLOW_NAME=go.yml